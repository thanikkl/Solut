<!-- <body style="background: linear-gradient(0deg, rgba(0,0,0,1) 0%, rgba(0,0,0,0.8) 20%, rgba(0,0,0,0.4) 100%),url('https://images.unsplash.com/photo-1512733596533-7b00ccf8ebaf?ixlib=rb-1.2.1&auto=format&fit=crop&w=1489&q=80'); background-size: cover; min-height: 100vh; opacity: 0.88;"> -->
<body style="background: linear-gradient(0deg, rgba(0,0,0,1) 0%, rgba(0,0,0,0.8) 20%, rgba(0,0,0,0.4) 100%),url('https://images.unsplash.com/photo-1505190849153-43410d3c18f8?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=2550&q=80'); background-size: cover; min-height: 100vh; opacity: 0.88;">
  <div class="container-profile">
    <h2 class="title-profile">EDIT MY PROFILE</h2>
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>
    <div class="profile-content">
      <div class="first-profile">
        <% if @user.profile_picture.attached? %>
          <%= image_tag(@user.profile_picture, class: "edit-picture-profile", id: "editProfile") %>
        <% else %>
          <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "edit-picture-profile", id: "editProfile" %>
        <% end %>
        <div class="upload-avatar">
          <%= f.input :profile_picture, as: :file, input_html: { class: 'd-none' }, label_html: { class: 'upload-photo'}, label: 'Change photo' %>
          <%= link_to "Delete my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: 'upload-photo delete_btn' %>
        </div>
        <div class="first-row">
          <%= f.input :nick_name, required: false %>
          <%= f.input :first_name, required: false %>
          <%= f.input :age, required: false %>
        </div>
      </div>
      <div class="second-profile">
        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
        <% end %>
        <%= f.input :email, required: false %>
        <%= f.input :address, required: false %>
        <%= f.input :genre_preferences, label: 'Repertory', collection: Event::GENRE_ARRAY  %>
        <div class="description-center">My Description</div>
        <%= f.text_area :description, required: false, rows: 3, cols: 47, label: 'Description', required: false  %>
        <%= f.input :current_password,
          # hint: "we need your current password to confirm your changes",
          required: true,
          input_html: { autocomplete: "current-password" } %>
      </div>
      <div class="third-profile">
        <!-- <div class="my-song">MY SONGS</div>
        <div class="my-media">MY VIDEOS</div> -->
        <div class="instrument-center">Add an instrument</div>
        <div class="instrument-profile">
          <!-- <#%= f.text_area :description, required: false, rows:5, cols: 28, label: 'Description', required: false  %> -->
          <!-- TODO - Access to "user-instrument" to get the instrument and the proficiency / système de tag ou select 2 plugin -->
          <!-- <#%= f.input :genre_preferences, label: 'Repertory', collection: ['Beginner', 'Moderate', 'Professional']  %> -->
          <!-- <#%= f.association :instruments, label: 'Instrument', selected: 37 %> -->
          <%= f.association :instruments, label_method: :name %>
          <%= f.association :user_instruments, label_method: :proficiency %>
        </div>
        <div class="instrument-center">My instruments</div>
        <div class="instrument-profile">
          <div class="instrument-text">
          Guitar (beginner) - delete
          </div>
        </div>
        <div class="bottom-container">
          <%= link_to "Delete my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: 'delete_btn' %> - <%= link_to "Back", :back, class: 'return_btn' %>
        </div>
      </div>
    </div>
    <div class="btn-position-profile">
      <%= f.button :submit, "Update", id: "profile-btn" %>
    </div>
    <% end %>
  </div>
</body>
